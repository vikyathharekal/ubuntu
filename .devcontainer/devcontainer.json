{
  "name": "Ubuntu Dev Container",
  "image": "ubuntu:latest",
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "postCreateCommand": "apt-get update && apt-get install -y vim",
  "extensions": [
    "ms-azuretools.vscode-docker",
    "ms-vscode.go"  // Add or remove any other extensions you need
  ],
  "features": {},
  "forwardPorts": [],
  "remoteUser": "root",
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": [
        "dbaeumer.vscode-eslint" // Additional extensions can be added here
      ]
    }
  }
}
